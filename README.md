# GhostList — Telegram-бот для парсинга подписчиков каналов

**GhostList** — бот-наблюдатель для администраторов Telegram-каналов. Позволяет выгружать список подписчиков канала в CSV-файл с расширенной информацией о пользователях.

> [!IMPORTANT]
> **Бот работает в режиме только для чтения (read-only).**
> Он **НЕ имеет** возможности удалять, банить или кикать пользователей из каналов. Бот только парсит данные подписчиков по заданным параметрам.

---

## 🔍 Возможности

- ✅ **Турбо-режим**: быстрая выгрузка подписчиков (до 10-20к за пару минут)
- ✅ **Интеграция с Supabase**: накопление базы подписчиков
- ✅ **Режим обогащения**: догрузка Bio и даты вступления только для тех, у кого их нет
- ✅ **Экспорт CSV**: выгрузка накопленной базы из облака
- ✅ Поддержка приватных каналов
- ✅ Интерактивный прогресс-бар

---

## ⚙️ Требования

- Python 3.7+
- Telegram API credentials (API_ID, API_HASH)
- Telegram Bot Token
- **Supabase Project** (URL + Key) — для хранения базы подписчиков

---

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/GhostList.git
cd GhostList
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Получите API-ключи Telegram

1. Перейдите на [https://my.telegram.org/apps](https://my.telegram.org/apps)
2. Войдите с вашим номером телефона
3. Создайте новое приложение (если ещё нет)
4. Скопируйте **API_ID** и **API_HASH**

### 5. Создайте бота через @BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте **Bot Token**

### 6. Настройте Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в SQL Editor и выполните скрипт:

```sql
CREATE TABLE subscribers (
    id BIGINT NOT NULL,
    channel_id BIGINT NOT NULL,
    username TEXT,
    first_name TEXT,
    last_name TEXT,
    phone TEXT,
    is_bot BOOLEAN DEFAULT FALSE,
    status TEXT,
    bio TEXT,
    join_date TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    PRIMARY KEY (id, channel_id)
);

CREATE INDEX idx_subscribers_channel ON subscribers(channel_id);
CREATE INDEX idx_subscribers_needs_enrichment ON subscribers(channel_id) 
    WHERE bio IS NULL OR join_date IS NULL;
```

3. Скопируйте **Project URL** и **anon public key** из настроек API.

### 7. Настройте переменные окружения

Создайте файл `.env` в корневой папке проекта:

```env
API_ID=12345678
API_HASH=ваш_api_hash_здесь
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
ALLOWED_USER_IDS=123456789,987654321
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=your-anon-key
```

---

## 🚀 Запуск

### Стандартный запуск

```bash
python run.py
```

### Альтернативный запуск (только бот без HTTP-сервера)

```bash
python main.py
```

---

## 📖 Использование

### Шаг 1: Добавьте бота в канал
1. Добавьте бота как администратора канала
2. Дайте боту права на просмотр участников

### Шаг 2: Добавьте канал в список
```
/addchannel @channelname
```

### Шаг 3: Работа с подписчиками
1. Введите `/channels` и выберите канал
2. **⚡ Турбо-режим**: запустите для быстрого сбора подписчиков. Если собрались не все — запустите ещё раз (бот доберет новых).
3. **📋 Обогащение**: запустите, чтобы получить Bio и дату подписки (это долго, но бот берет только тех, у кого этих данных нет).
4. **📥 Экспорт CSV**: скачайте готовый файл из базы.

---

## 📊 Формат CSV-файла

| Поле | Описание |
|------|----------|
| ID | Telegram ID |
| Username | @username |
| First Name | Имя |
| Last Name | Фамилия |
| Phone | Телефон |
| Bot | Бот? (Да/Нет) |
| Status | Статус (Online, Offline...) |
| Bio | Биография |
| Join Date | Дата вступления |

---

## 👤 Автор

GhostList v2.0.0
