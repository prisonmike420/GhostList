# GhostList — Telegram-бот для парсинга подписчиков каналов

**GhostList** — бот-наблюдатель для администраторов Telegram-каналов. Позволяет выгружать список подписчиков канала в CSV-файл с расширенной информацией о пользователях.

> [!IMPORTANT]
> **Бот работает в режиме только для чтения (read-only).**
> Он **НЕ имеет** возможности удалять, банить или кикать пользователей из каналов. Бот только парсит данные подписчиков по заданным параметрам.

---

## 🔍 Возможности

- ✅ Парсинг подписчиков привязанного канала
- ✅ Экспорт данных в CSV-файл
- ✅ Расширенная информация о пользователях:
  - ID, username, имя, фамилия
  - Биография (bio)
  - Дата вступления в канал
  - Флаги: Premium, Bot, Deleted, Scam, Fake
- ✅ Поддержка приватных каналов
- ✅ Прогресс-бар выгрузки с возможностью отмены
- ✅ Удобный inline-интерфейс

---

## ⚙️ Требования

- Python 3.7+
- Telegram API credentials (API_ID, API_HASH)
- Telegram Bot Token

---

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/GhostList.git
cd GhostList
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Получите API-ключи Telegram

1. Перейдите на [https://my.telegram.org/apps](https://my.telegram.org/apps)
2. Войдите с вашим номером телефона
3. Создайте новое приложение (если ещё нет)
4. Скопируйте **API_ID** и **API_HASH**

### 5. Создайте бота через @BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте **Bot Token**

### 6. Настройте переменные окружения

Создайте файл `.env` в корневой папке проекта:

```env
API_ID=12345678
API_HASH=ваш_api_hash_здесь
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
```

---

## 🚀 Запуск

### Стандартный запуск

```bash
python run.py
```

### Альтернативный запуск (только бот без HTTP-сервера)

```bash
python main.py
```

### Проверка подключения к API

```bash
python test_connection.py
```

При успешном запуске вы увидите:
```
GhostList v1.2.0 запускается...
HTTP-сервер запущен на порту 3000
Клиент MTProto запущен
GhostList готов к работе!
```

---

## 📖 Использование

### Шаг 1: Добавьте бота в канал

1. Откройте настройки вашего канала
2. Перейдите в "Administrators" (Администраторы)
3. Добавьте бота как администратора
4. Дайте боту права на просмотр участников

### Шаг 2: Запустите бота

Напишите боту команду `/start`

### Шаг 3: Добавьте канал в список

```
/addchannel @channelname
```

или для приватных каналов:

```
/addchannel -100123456789
```

> 💡 ID приватного канала можно получить, переслав сообщение из канала боту [@userinfobot](https://t.me/userinfobot)

### Шаг 4: Выгрузите подписчиков

1. Введите `/channels` или нажмите кнопку "📋 Список каналов"
2. Выберите канал
3. Дождитесь завершения парсинга
4. Получите CSV-файл с данными

---

## 📌 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/channels` | Показать список добавленных каналов |
| `/addchannel` | Добавить канал (формат: `/addchannel @username` или `/addchannel -100123456789`) |
| `/removechannel` | Удалить канал из списка отслеживаемых |
| `/help` | Показать справку |
| `/cancel` | Отменить текущую операцию |

---

## 📊 Формат CSV-файла

Экспортируемый файл содержит следующие поля:

| Поле | Описание |
|------|----------|
| ID | Telegram ID пользователя |
| Username | @username пользователя |
| First Name | Имя |
| Last Name | Фамилия |
| Phone | Номер телефона (если доступен) |
| Bot | Является ли пользователь ботом |
| Deleted | Удалённый ли аккаунт |
| Premium | Подписка Telegram Premium |
| Bio | Биография пользователя |
| Scam | Метка "мошенник" от Telegram |
| Fake | Метка "фейк" от Telegram |
| Join Date | Дата вступления в канал |

---

## 🛡️ Безопасность и ограничения

### Что бот МОЖЕТ делать:
- ✅ Получать список подписчиков канала
- ✅ Читать публичную информацию о пользователях
- ✅ Экспортировать данные в CSV

### Что бот НЕ МОЖЕТ делать:
- ❌ Удалять пользователей из канала
- ❌ Банить пользователей
- ❌ Кикать пользователей
- ❌ Изменять права пользователей
- ❌ Рассылать сообщения подписчикам

---

## 🗂️ Структура проекта

```
GhostList/
├── main.py              # Основной код бота
├── run.py               # Точка входа с HTTP-сервером
├── server.py            # HTTP-сервер для мониторинга
├── test_connection.py   # Тестирование подключения к API
├── requirements.txt     # Зависимости Python
├── .env                 # Переменные окружения (созданы вами)
└── data/
    ├── channels.json    # Сохранённые каналы
    └── bot_session.*    # Сессия MTProto
```

---

## 🐛 Устранение неполадок

### Бот не подключается к Telegram API

1. Проверьте правильность `API_ID` и `API_HASH` в `.env`
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` корректен
3. Проверьте интернет-соединение

### Ошибка "Бот не является администратором канала"

1. Убедитесь, что бот добавлен как администратор канала
2. Проверьте, что у бота есть права на просмотр участников

### Не удаётся получить подписчиков

1. Для каналов с большим количеством подписчиков процесс может занять время
2. Telegram API имеет ограничения на количество запросов
3. Попробуйте позже, если получаете ошибки rate limit

---

## 📜 Лицензия

MIT License

---

## 👤 Автор

GhostList v1.2.0
